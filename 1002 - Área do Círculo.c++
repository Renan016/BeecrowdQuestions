#include <bits/stdc++.h>

using namespace std;

int main()
{

    double area, n = 3.14159, raio;
    cin >> raio;
        area = n*(raio*raio);
        cout << "A=" << fixed << setprecision(4) << area << endl;
    return 0;
}